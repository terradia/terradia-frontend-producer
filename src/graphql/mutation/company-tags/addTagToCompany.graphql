mutation addTagToCompany($companyId: String!, $companyTagId: String!) {
    addTagToCompany(companyId: $companyId, companyTagId: $companyTagId) {
        id
        tags {
            id
            translationKey
            color
        }
    }
}