mutation deleteCompany($companyId: String!) {
    deleteCompany(companyId: $companyId) {
        id
        name
        description
        email
        phone
        address
        archivedAt
    }
}