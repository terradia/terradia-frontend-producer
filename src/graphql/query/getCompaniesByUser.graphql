query getCompaniesByUser($userId: ID!) {
    getCompaniesByUser(userId: $userId) {
        id
        company {
            id
            name
            description
            address
            phone
            logo {
                id
                filename
            }
            cover {
                id
                filename
            }
        }
    }
}