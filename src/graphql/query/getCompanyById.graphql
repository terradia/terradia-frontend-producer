query getCompanyById ($companyId: ID!) {
    getCompany(companyId: $companyId) {
        id
        name
        description
        email
        phone
        address
        logo {
            id
            filename
        }
        cover {
            id
            filename
        }
        openingDays {
            id
            daySlugName
            dayTranslationKey
            hours {
                id
                startTime
                endTime
            }
        }
    }
}