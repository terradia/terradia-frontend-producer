query getCompanyById ($companyId: ID!) {
    getCompany(companyId: $companyId) {
        id
        name
        description
        email
        phone
        logo
        cover
        address
        openingDays {
            id
            daySlugName
            dayTranslationKey
            hours {
                id
                startTime
                endTime
            }
        }
    }
}