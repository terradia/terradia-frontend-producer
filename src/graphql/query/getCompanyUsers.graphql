query getCompanyUsers($companyId: ID!) {
  getCompany(companyId: $companyId) {
    users {
      id
      user {
        email
        firstName
        lastName
        id
      }
      roles {
        id
        slugName
      }
    }
  }
}
